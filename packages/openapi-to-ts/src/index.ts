export function openapiToTs(input: string, output: string, options?: Record<string, unknown>) {
  console.log(
    `[Coming soon...] Generate types from '${input}' to '${output}' directory with these options:`,
    JSON.stringify(options)
  )
}
